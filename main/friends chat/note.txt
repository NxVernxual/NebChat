all files has to be in the same folder in order to work properly.
if a file like "index v2" was moved out. then it wont display the css.
so keep everything inside the folder

index v1 has the css in it and doesnt need an extrenal css file
index v2 is linked to the css and js files which needs the files